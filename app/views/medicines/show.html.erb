<p id="notice"><%= notice %></p>

<p>
  <strong>名前:</strong>
  <%= @medicine.name %>
</p>

<p>
  <strong>効用:</strong>
  <%= @medicine.effect %>
</p>

<p>
  <strong>頻度:</strong>
  <% @medicine.frequencies.each do |frequency| %>
    <%= frequency.name %>
  <% end %>
</p>

<%= button_to '編集', edit_medicine_path(@medicine),:method => :get %> 
<%= button_to '戻る', new_medicine_path,:method => :get %>
<%= button_to "毎日のおくすり記録", histories_path,:method => :get %>